$(document).ready(function() {          // When page is Load go top of the page
          $('body,html').animate({
              scrollTop : 0                   // Scroll to top of body
          }, 0);
});
